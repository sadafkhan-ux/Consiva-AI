"""Phase 2 stub. Feature extraction will be built from reviewed `approvals` history
once there's enough of it — not designed blind ahead of that data (docs/architecture §10)."""


def extract_features(item: dict) -> dict:
    raise NotImplementedError("Feature extraction is a Phase 2 concern — no labelled data exists yet")
